import { TicketUpdateRequest } from './ticket-update-request';

describe('TicketUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new TicketUpdateRequest()).toBeTruthy();
  });
});
