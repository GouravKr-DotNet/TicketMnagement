import { TicketUpdateresponse } from './ticket-updateresponse';

describe('TicketUpdateresponse', () => {
  it('should create an instance', () => {
    expect(new TicketUpdateresponse()).toBeTruthy();
  });
});
